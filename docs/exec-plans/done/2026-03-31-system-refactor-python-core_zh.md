# 执行计划：Python 核心系统重构

- 日期：2026-03-31
- 任务 UUID：f731efc3-eb41-48f8-959e-b4a518abf941

## 目标
在不改动 `server/` 与已弃用 `news_module/` 的前提下，重构项目 Python 侧为更清晰的分层架构，并补齐可维护性、兼容性与完整自动化测试。

## 范围
- 范围内：
  - 重建配置、运行状态、任务执行、流水线编排等核心架构。
  - 将 `scheduler/`、`scripts/`、`data_pipeline/`、`alpha_models/` 的调用路径迁移到共享服务层。
  - 尽量保持现有 CLI 行为兼容。
  - 增加单元测试与整体流程测试（使用小规模数据与重操作 mock）。
  - 同步更新中英文文档并与代码一致。
- 范围外：
  - `server/` C++ 网关重构。
  - `news_module/` 功能恢复（当前为弃用模块）。

## 假设
- 当文档与代码冲突时，以当前代码与测试行为为准。
- 获取全市场数据与真实模型训练属于重操作，自动化测试使用轻量替代与打桩。
- 工作区存在与本任务无关的既有改动，不进行回滚。

## 步骤
1. 新增核心模块：统一设置、运行历史持久化、任务接口与流水线执行器。
2. 将调度任务重构到共享服务层，去除重复编排逻辑。
3. 将脚本重构为“薄 CLI 包装 + 服务层入口”。
4. 修复基线已知脆弱点（`ingest` 文件清理耦合、cooldown 静态读取）。
5. 补齐/升级单元测试与整体流程测试（全流程 mock）。
6. 更新架构与使用文档（中英文）。
7. 输出结构化中英文任务日志，并完成后移动计划到 `docs/exec-plans/done/`。

## 验收标准
- Python 架构完成模块化分层，职责清晰。
- 现有命令入口仍能触发预期任务。
- 重构后具备完整单元测试与整体流程测试覆盖。
- 基线中的已有失败项被修复。
- 文档为中英文双语且 UTF-8 编码，与实现一致。

## 回滚说明
- 回滚以下目录中的重构文件：
  - `config/`、`scheduler/`、`scripts/`、`data_pipeline/`、`alpha_models/`、`utils/`、`test/` 及相关文档。
- 回滚 `main.py` 与 scheduler 相关模块可恢复旧编排。
- 若完全回滚，同时删除本 UUID 的计划与日志文件。
