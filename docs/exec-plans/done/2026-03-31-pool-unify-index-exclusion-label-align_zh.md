# 执行计划：股票池逻辑复用 + 预测池排除指数 + 标签对齐

- 日期：2026-03-31
- 任务 UUID：f7968da5-dae1-4754-893f-acf535b97fd8

## 目标
将训练池与预测池中的共有预处理步骤提取为可复用函数，确保预测池不包含指数，并让预测标签表达式与训练标签保持一致。

## 范围
- 范围内：
  - 在 `utils/` 下提取股票池采样/流动性相关共有逻辑。
  - 在 `scripts/filter.py` 与 `scripts/predict.py` 中复用该逻辑。
  - 预测池生成和“前一日补池”都排除指数代码。
  - 预测标签与训练配置标签对齐。
  - 增加或更新针对性测试。
- 范围外：
  - 股票池策略的大幅重构。
  - 模型结构或训练流程重设计。

## 假设
- `settings.data_path/index_code_list` 是本地指数代码权威来源。
- `alpha_models/workflow_config_transformer_Alpha158.yaml` 中当前训练标签语义为基准。
- 优先最小、可回滚改动。

## 步骤
1. 在 `utils/` 增加共有函数（分层采样、标签常量等）。
2. 重构 `scripts/filter.py` 与 `scripts/predict.py` 以复用共有函数。
3. 在预测池排名阶段和前一日补池阶段增加指数过滤。
4. 统一预测标签字符串到训练标签常量。
5. 增加/调整单元测试覆盖复用、指数过滤、标签对齐。
6. 运行最小必要测试并验证。

## 验收标准
- 预测池不包含 `index_code_list` 内的指数代码。
- 训练与预测使用同一 Alpha158 标签表达式。
- 共有逻辑只实现一次，并被训练池与预测池路径复用。
- 相关测试通过。

## 回滚说明
- 回滚本次涉及文件：
  - `utils/preprocess.py`
  - `scripts/filter.py`
  - `scripts/predict.py`
  - `test/` 下新增/修改测试
- 若取消实施，删除本计划文档。
