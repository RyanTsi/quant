qlib_init:
    provider_uri: "~/Documents/quant/.data/qlib_data"
    region: cn
market: &market my_800_stocks
benchmark: &benchmark SH000001
data_handler_config: &data_handler_config
    start_time: 2010-04-01
    end_time: 2026-03-26
    fit_start_time: 2010-04-01
    fit_end_time: 2024-03-26
    instruments: *market
    infer_processors:
        # - class: FilterCol
        #   kwargs:
        #       fields_group: feature
        #       col_list: ["RESI5", "WVMA5", "RSQR5", "KLEN", "RSQR10", "CORR5", "CORD5", "CORR10", 
        #                     "ROC60", "RESI10", "VSTD5", "RSQR60", "CORR60", "WVMA60", "STD5", 
        #                     "RSQR20", "CORD60", "CORD10", "CORR20", "KLOW"
        #                 ]
        - class: RobustZScoreNorm
          kwargs:
              fields_group: feature
              clip_outlier: true
        - class: Fillna
          kwargs:
              fields_group: feature
    learn_processors:
        - class: DropnaLabel
        - class: CSRankNorm
          kwargs:
              fields_group: label
    label: ["(5 * (Ref($close, -2)/Ref($close, -1) - 1) + 4 * (Ref($close, -3)/Ref($close, -1) - 1) + 3 * (Ref($close, -4)/Ref($close, -1) - 1) + 2 * (Ref($close, -5)/Ref($close, -1) - 1) + 1 * (Ref($close, -6)/Ref($close, -1) - 1)) / 15"]

    
port_analysis_config: &port_analysis_config 
    strategy:
        class: TopkDropoutStrategy
        module_path: qlib.contrib.strategy
        kwargs:
            signal: <PRED>
            topk: 50
            n_drop: 5
    backtest:
        start_time: 2025-03-27
        end_time: 2026-03-26
        account: 100000000
        benchmark: *benchmark
        exchange_kwargs:
            # limit_threshold: 0.095
            deal_price: close
            open_cost: 0.0005
            close_cost: 0.0015
            min_cost: 5
task:
    model:
        class: TransformerModel
        module_path: qlib.contrib.model.pytorch_transformer_ts
        kwargs:
            seed: 42
            n_jobs: 4
            batch_size: 4096
            d_feat: 158
            d_model: 64
            nhead: 4
            num_layers: 3
            dropout: 0.1

    dataset:
        class: TSDatasetH
        module_path: qlib.data.dataset
        kwargs:
            handler:
                class: Alpha158
                module_path: qlib.contrib.data.handler
                kwargs: *data_handler_config
            segments:
                train: [2010-04-01, 2024-03-26]
                valid: [2024-03-27, 2025-03-26]
                test: [2025-03-27, 2026-03-26]
            step_len: 20
    record: 
        - class: SignalRecord
          module_path: qlib.workflow.record_temp
          kwargs: 
            model: <MODEL>
            dataset: <DATASET>
        - class: SigAnaRecord
          module_path: qlib.workflow.record_temp
          kwargs: 
            ana_long_short: False
            ann_scaler: 252
        - class: PortAnaRecord
          module_path: qlib.workflow.record_temp
          kwargs: 
            config: *port_analysis_config
